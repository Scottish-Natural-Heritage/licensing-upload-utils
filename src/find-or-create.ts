/**
 * @typedef {object} ObjectiveRef Represents information on a object that has been created on Objective.
 * @property {string} objectiveId  The identifier of the object in Objective.
 */
type ObjectiveRef = {
  objectiveId: string;
};

export type {ObjectiveRef};
